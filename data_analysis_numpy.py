import numpy as np

# Example dataset
scores = np.array([7.7, 6.9, 5.5, 8.1, 7.2])

print("Mean score:", np.mean(scores))
print("Median score:", np.median(scores))
print("Standard deviation:", np.std(scores))
print("Sorted scores:", np.sort(scores))
